ALTER TABLE public.photos ADD COLUMN IF NOT EXISTS is_favorite boolean NOT NULL DEFAULT false;
CREATE INDEX IF NOT EXISTS photos_is_favorite_idx ON public.photos (is_favorite) WHERE is_favorite = true;